name: demo
on: [push]
jobs: 
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: welcome message
              run: echo "hello"
            - name: list_files
              run: ls
            - name: read file
              run: cat README.md
